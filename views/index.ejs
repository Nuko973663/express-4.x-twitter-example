<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, shrink-to-fit=no, viewport-fit=cover"
    />
    <link rel="stylesheet" href="/stylesheets/pico.css" />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <script src="/js/jquery-3.6.0.min.js"></script>
    <script src="/js/ethers-5.6.4.umd.min.js"></script>
    <title>Address register</title>

    <style>
      /* Global CSS variables */
      :root {
        --spacing: 3rem;
        --text-weight: 400;
        --titles-weight: 800;
        --block-round: 0;
      }

      /* Typography */
      h2,
      h3,
      hgroup > :last-child {
        font-weight: 200;
      }
      small {
        color: var(--muted-text);
      }

      hgroup {
        background: rgba(0, 0, 0, 0.5);
        padding: 1em;
      }

      /* Header */

      :root:not([data-theme="dark"]),
      [data-theme="light"] {
        --nav-background: rgba(255, 255, 255, 0.7);
        --nav-border: rgba(115, 130, 140, 0.2);
      }

      [data-theme="dark"] {
        --nav-background: rgba(16, 24, 30, 0.8);
        --nav-border: rgba(115, 130, 140, 0.2);
      }
    </style>
  </head>

  <body>
    <%- include('header'); -%>

    <div class="hero">
      <header class="container">
        <hgroup>
          <h1>Address register</h1>
          <h2>Login with Twitter and connect metamask</h2>
        </hgroup>
      </header>
    </div>

    <main class="container">
      <section>
        <div>
          <% if (user) { %>
          <hgroup>
            <h4>Twitter</h4>
            <p>
              <small>@<%= user.username %> - <%= user.displayName %></small>
            </p>
          </hgroup>

          <hgroup>
            <h4>Ethereum Address</h4>
            <p>
              <% if (address) { %>
              <small>@<%= address %></small><br /><br />
              <button id="connect">Update Address</button>
              <% } else { %>
              <button id="connect">Connect Metamask</button>
              <% } %>
            </p>
          </hgroup>

          <% if (role) { %>
          <hgroup>
            <h4>Role</h4>
            <p>
              <small>Admin: <%= role.admin %></small>
            </p>
            <p>
              <small>Referee: <%= role.referee %></small>
            </p>
          </hgroup>
          <% } %> <% } else { %>
          <p>
            <a href="/login/federated/twitter.com" role="button"
              >Sign in with Twitter</a
            >
          </p>
          <% } %>
        </div>
      </section>
    </main>

    <footer class="container">
      <small>@nuko973663</small>
    </footer>
    <script src="/js/ar.js"></script>
  </body>
</html>
